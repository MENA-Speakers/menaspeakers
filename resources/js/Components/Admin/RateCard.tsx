import React from 'react';
import {RateCardType} from "@/types/rate-card";

function RateCard({rateCard}: { rateCard: RateCardType}) {
  return (
    <div>

    </div>
  );
}

export default RateCard;
