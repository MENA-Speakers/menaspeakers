import React from 'react';
import {RateCardType} from "@/types/rate-card";

interface PptxSlideProps {
  rateCards: RateCardType[],
}

function PptxSlide({rateCards}: PptxSlideProps) {
  return (
    <>

    </>
  );
}

export default PptxSlide;
