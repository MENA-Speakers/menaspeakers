// Video type interface

export interface VideoType {
    id: number;
    profile_id: number;
    link: string;
}

// Gallery type interface

export interface GalleryType {
  id?: number;
  url?: string;
}
